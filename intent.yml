goal: |
  Please write a TypeScript function called `getCatPhotos` that returns a list of 3 cat image URLs.
  Use the tool `fs_guard` to write this code to "./workspace/output.ts"
constraints:
  - must use TypeScript
  - must define a function named getCatPhotos
